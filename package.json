{
  "name": "momentum-frontpage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint --fix",
    "lint:check": "eslint",
    "format": "prettier . --write && stylelint ./**/*.{css,scss} --fix",
    "format:check": "prettier . --check && stylelint ./**/*.{css,scss} --fix"
  },
  "lint-staged": {
    "{src,lib,tests,config}/**/*.{js,md}": [
      "eslint --quiet --fix",
      "prettier --write"
    ]
  },
  "devDependencies": {
    "eslint": "^8.37.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-plugin-prettier": "^4.2.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.0",
    "prettier": "^2.8.7",
    "sass": "^1.60.0",
    "stylelint": "^14.16.1",
    "stylelint-config-prettier": "^9.0.5",
    "stylelint-config-recommended": "^9.0.0",
    "stylelint-config-sass-guidelines": "^9.0.1",
    "stylelint-prettier": "^3.0.0",
    "stylelint-scss": "^4.6.0",
    "vite": "^4.2.0"
  }
}
